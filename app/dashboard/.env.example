# Copy this file to .env and fill in real values
# Backend API base URL (Bruno's FastAPI)
VITE_API_BASE=http://localhost:8000

# MQTT broker (optional — for direct subscribe; usually backend handles MQTT)
VITE_MQTT_BROKER=wss://mqtt.zwehopark.rw:8884
